<%= form_for @property_type, :html => { :class => "form-horizontal property_type" } do |f| %>

    <% if @property_type.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@property_type.errors.count, "error") %> prohibited this property_type from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @property_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@property_type[:name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            property_types_path, :class => 'btn btn-default' %>

<% end %>
